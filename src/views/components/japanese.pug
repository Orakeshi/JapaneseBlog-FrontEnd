mixin furigana(characters)
    each character in characters 
        each value, key in character
            ruby.japanese-text
                = key
                rt.furigana #{value}

//- mixin translationFurigana(translationCharacters)
    
//-     each translationCharacter in translationCharacters
//-         if typeof translationCharacter == 'string'
//-             | #{translationCharacter}(
//-         else if Array.isArray(translationCharacter)
//-             each character in translationCharacter
                
//-                 each value, key in character
//-                     ruby.japanese-text
//-                         = key
//-                         rt.furigana #{value}
//-             | )

mixin translationFurigana(translation, translationCharacters)
    | #{toggleElement}(
    +furigana(translationCharacters)
    | )

//- ruby.japanese-text
//-     | 平 
//-     rt.furigana ひら
//-     | 仮 
//-     rt.furigana が
//-     | 名 
//-     rt.furigana な